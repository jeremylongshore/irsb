# IRSB Solver Configuration
# Copy to .env and adjust values

# Environment
NODE_ENV=development
LOG_LEVEL=info

# Data directory
DATA_DIR=./data

# Optional: Path to intent fixture for testing
# INTENT_FIXTURE_PATH=./fixtures/intents/sample-safe-report.intent.json

# Policy settings
POLICY_JOBTYPE_ALLOWLIST=SAFE_REPORT
POLICY_MAX_ARTIFACT_MB=5
# Optional: comma-separated list of allowed requesters
# POLICY_REQUESTER_ALLOWLIST=user1@example.com,user2@example.com

# Storage paths (computed from DATA_DIR if not set)
# RECEIPTS_PATH=./data/receipts.jsonl
# REFUSALS_PATH=./data/refusals.jsonl
# EVIDENCE_DIR=./data/evidence

# Optional: Path to JSON config file (overrides env vars)
# IRSB_SOLVER_CONFIG_PATH=./config.json
